# 使用情境與雙語維護

本次範圍：六個既有繁中／英文頁面。交班系統唯一公開情境為 `rehab-home`（康復之家／Psychiatric Rehabilitation Home）。產品名稱、title、H1、canonical、URL 與產品圖片順序維持。

## 情境結構

- `js/handover-scenarios.js`：公開情境清單、`published`、雙語標籤與內容資料。
- `js/handover.js`：依 `?scenario=rehab-home` 選擇內容，處理選擇、語言切換、上一頁／下一頁及未知情境回復。
- 兩份交班 HTML 的 `data-scenario-copy` 對應情境資料內的固定 key，涵蓋 Hero 說明、功能與服務對象用語、CTA、版本說明及聯絡邀請。key 是穩定識別，不因位置調整而重新編號。
- 原始 HTML 保留完整預設內容，JavaScript 不可用時仍可閱讀、下載與切換語言；目前唯一情境仍以原生選擇器呈現。
- 選擇器採網站字體、色彩、邊框、圓角與焦點樣式；下拉操作使用瀏覽器原生鍵盤及手機選擇介面。

## 日後新增情境

1. 在同一份 registry 新增唯一 id，填妥 `zh-TW` 與 `en-GB` 的 `label`、完整 `content`。本次未建立任何未來產業資料。
2. `content` 各 key 提供 `html`；CTA 另提供 `href`。僅可放已審閱的本站公開內容，禁止把 URL 或其他外部輸入當成 HTML。
3. 確認雙語產品事實、用語、CTA、圖片說明與版本狀態一致，才設為 `published: true`。未公開情境不列入選單。
4. 若增加新的可切換欄位，為既有標記加上穩定的 `data-scenario-copy`，同步補齊各情境兩語資料。預設情境內容與静態 HTML 須保持一致。
5. 切換語言沿用同一情境 id、其他查詢參數與頁內錨點。未知或未公開 id 回復公開預設值；不新增情境路徑或變更產品 SEO 主體。
6. 更新 CSS／情境 JS 後，更新 HTML 中各資源的內容雜湊版本參數。

## 本次雙語校正

- 六頁一致提供 Email 與 LINE ID，補齊英文首頁及英文 EMBERLITE。
- 首頁交班卡移除舊「PRODUCT IMAGE COMING SOON」文字，保留版型並呈現交班／事件／後續處理。
- 首頁統一服務對象事件用語，繁中補齊一般事項。
- EMBERLITE 英文補齊主機光源控制、曝光結束與推估曲線說明，將 lens 校正為對應焦距的 focal length；繁中圖片替代文字與原圖連結說明完成在地化。
- 交班 DEMO 維持 Windows／繁體中文／V1.3.13，同一下載網址；正式版本仍未發行。EMBERLITE 保留既有功能與展示順序，不新增下載或發行承諾。

## 驗證

本機 Chrome：六頁 × 320／375／768／1440px，共 24 組通過。檢查水平溢位、圖片載入、原圖順序、站內連結與錨點、語言往返、聯絡資訊、手機導覽及執行錯誤。另測情境保留、未知值回復、未公開情境隱藏、瀏覽歷史與無 JavaScript 預設內容。以僅存在測試瀏覽器記憶體的情境驗證同一 renderer 能切換所有內容綁定，未將測試情境放入網站。已檢視桌機與手機 Hero 截圖。

DEMO 公開下載端點回傳 HTTP 206，ZIP 檔頭為 `504b0304`；未修改或執行 DEMO。測試腳本、截圖與詳細結果保留在不部署的 `.preview/scenario-*`。
